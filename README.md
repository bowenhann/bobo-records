# Bobo Records

一个 Public Records 气质的个人静态网站：左侧是个人介绍和漂浮图片，右侧两个竖向页面分别是鸡尾酒配方和音乐乐评。

## 本地预览

```bash
python3 -m http.server 5173
```

打开：

```text
http://localhost:5173
```

## 图片入口

页面右下角的 `Edit Images` 可以修改漂浮图片链接和 caption，内容会保存到当前浏览器的 `localStorage`。
